[project]
name = "qa-kit-tools"
version = "0.1.0"
description = "Standalone tools for the qa-kit plugin (export, validate, harness)"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "openpyxl>=3.1",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
